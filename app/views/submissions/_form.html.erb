<%= form_for(@submission, :html => { :multipart => true }) do |f| %>

  <%= f.error_messages %>
  
  <div class="field">
    <p>
      <%= f.label :title %><br />
      <%= f.text_field :title %>
      <%= f.hidden_field :conference_id, :value => params[:conference_id] %>
    </p>
  </div>
  
  <div class="field">
    <p>
      <%= f.label :body %><br />
      <%= f.text_area :body %>
    </p>
  </div>
  
  <div class="field">
    <p>
      <%= f.label :image %>
      <%= f.file_field :image %>
    </p>
  </div>

  <%= f.submit %>
<% end %>