<% title @submission.title %>





<%= @submission.body %>

<% unless @submission.image.nil? %>
  <%= image_tag @submission.image.url %>
<% end %>

<p id="pdf_link">
  <%= link_to "PDF Version", submission_url(@submission, :format => "pdf") %>
</p>
