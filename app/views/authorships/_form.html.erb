<%= form_for([@submission, @authorship]) do |f| %>
  <%= f.error_messages %>
  
  <div class="field">
    <%= f.collection_select :author_id, Author.all, :id, :full_name, :prompt => "Select an author." %>
  </div>
  
  <div class="actions">
    <%= f.submit "Add the author" %>
  </div>
  
<% end %>